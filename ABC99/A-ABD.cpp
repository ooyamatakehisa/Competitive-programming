#include<iostream>
#include <math.h>
#include <algorithm>
#include<queue>
#include <bits/stdc++.h>


using namespace std;

#define FOR(i,a,b) for(int i=(a),i##formax=(b);i<i##formax;i++)

main(){
    int N;
    cin >> N;
    if(N >= 1000){
        cout << "ABD"  << endl;
    }   
    else{
        cout << "ABC"  << endl;
    }
}